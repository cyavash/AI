# AI

This repository collects experiments and utilities for AI-related projects.

## Project Structure

- `utils.py` - simple mathematical utilities.
- `theme/` - contains UI resources like `layout.css` implementing a 12-column CSS grid.
- `tests/` - pytest-based unit tests.

